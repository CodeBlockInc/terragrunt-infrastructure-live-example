pipeline {
    agent any

    stages {
        stage('Plan') {
            steps {
	    	dir('non-prod') {
		    sh 'terragrunt'
		}
            }
        }
        stage('Apply') {
            steps {
                dir('non-prod') {
                    sh 'terragrunt'
		}
            }
        }
    }
}

